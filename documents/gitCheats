# clone the repository (from github, for example)
#First go to the directory where you want to download the files
git clone https://github.com/peidevs/MEAN-GovernmentServiceAlerts.git  

# edit some files

# add them to the index
git add file1.txt
git add file2.gif
#add all files to index/commit
git add .

# review your changes
git status    

# commit the changes
git commit -m "Description of my change"

# push them back to the remote repository (Github)
git push origin master


# get latest version
git pull
